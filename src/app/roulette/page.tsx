// Remove unused imports
import { /* Timer, Coins, History */ } from "lucide-react";
import {
  // Remove unused components
  // CardContent,
  // CardHeader,
  // CardTitle,
} from "@/components/ui";

// For the TrollBox error, make sure to import it:
import { TrollBox } from "@/components/troll-box"; 